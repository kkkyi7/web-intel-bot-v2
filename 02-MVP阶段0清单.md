# 阶段 0 · MVP 实施清单（零代码走通版）

> **目标**：2 周内跑通"YouTube 供应链主题视频 → AI 摘要 → 每日邮件"的最小闭环
> **前置条件**：零代码基础可完成 95% 的步骤，只需要一次性复制粘贴几段配置
> **预计投入**：首次搭建 4–6 小时 · 之后每天 0 分钟（全自动）
> **预计月成本**：¥0–50（前两个月）→ 稳定后 ¥50–150

---

## 本文件你要怎么用

按章节顺序做，**每个章节做完打勾再进下一个**。不要跳跃。

- [ ] 第 1 章：准备账号（1 小时）
- [ ] 第 2 章：部署 n8n（30 分钟，推荐 Cloud 方案）
- [ ] 第 3 章：配置凭证（20 分钟）
- [ ] 第 4 章：搭建工作流（90 分钟，核心）
- [ ] 第 5 章：测试与上线（30 分钟）
- [ ] 第 6 章：第一周观察（每天 3 分钟）
- [ ] 第 7 章：阶段 0 验收与毕业

---

## 第 1 章 · 准备账号清单

### 1.1 必须的 4 个账号

| # | 账号 | 用途 | 是否收费 | 申请入口 |
|---|---|---|---|---|
| 1 | **Google Cloud 账户** | 拿 YouTube Data API Key | 免费（个人用量够） | console.cloud.google.com |
| 2 | **Anthropic Console** | 拿 Claude API Key | 充值起步，用多少扣多少 | console.anthropic.com |
| 3 | **Gmail**（或 Outlook/163） | 发日报邮件的发件人 | 免费 | gmail.com |
| 4 | **n8n Cloud**（推荐）或自建 | 跑工作流 | 试用 14 天免费，之后 €20/月 | n8n.io/cloud |

> **为什么推荐 n8n Cloud 而不是自建？** 自建要买云主机、装 Docker、搞反向代理、维护升级——你"只懂一点点"代码，自建的时间成本远高于每月 €20。等阶段 2 再考虑自建。

### 1.2 账号申请细节

#### A. Google Cloud · YouTube Data API

1. 打开 console.cloud.google.com，用 Google 账号登录（没有就注册一个）
2. 顶部点"选择项目" → "新建项目" → 项目名填 `web-intel-bot` → 创建
3. 左侧菜单 → "API 和服务" → "库" → 搜索 `YouTube Data API v3` → 点进去 → 启用
4. 左侧菜单 → "API 和服务" → "凭据" → "创建凭据" → "API 密钥"
5. **复制生成的 API Key**，存到一个安全的地方（比如 1Password 或记事本）
6. 点"限制密钥" → 只勾选 `YouTube Data API v3` → 保存（防止别人偷用）

> **配额说明**：YouTube API 每天给你 10,000 点配额。一次搜索约 100 点、一次取视频详情 1 点。我们的用法每天约 500 点，绰绰有余。

#### B. Anthropic · Claude API

1. 打开 console.anthropic.com，注册/登录
2. 左侧 "API Keys" → "Create Key" → 名字写 `web-intel-bot` → Create
3. **复制 Key（只显示一次，关了就看不到）**
4. 左侧 "Billing" → "Add payment method" → 绑卡（支持 Visa/Mastercard，不支持国内大部分借记卡，**用信用卡**）
5. 预充值 $10（够用 1–2 个月）

> **没有国际信用卡怎么办？**
> - 方案 A：用 OpenRouter（openrouter.ai），支持支付宝充值，可以用 Claude 模型，会多 5% 手续费
> - 方案 B：用国内大模型替代：DeepSeek、通义千问（qwen-max）、Kimi——性价比高，n8n 都有节点。阶段 0 我们主要是做摘要，国内模型完全够用

#### C. Gmail（或国内邮箱）

- 用已有的就行。如果用 Gmail，需要在账号设置里打开 "两步验证"，然后生成一个"应用专用密码"给 n8n 用
- 如果用 163/QQ 邮箱，需要在邮箱设置里开启 SMTP 服务，记住生成的"授权码"

### 1.3 账号准备完成后的清单

你手上应该有这些信息（填到你的密码管理器或一张安全的纸上）：

```
[ ] YouTube API Key:           AIzaSy...
[ ] Claude API Key:            sk-ant-...
[ ] n8n Cloud 登录邮箱/密码:    xxx@gmail.com
[ ] 发件邮箱地址:               xxx@gmail.com
[ ] 发件邮箱应用密码:           xxxx xxxx xxxx xxxx
[ ] 收件邮箱地址（可以是同一个）: xxx@gmail.com
```

---

## 第 2 章 · 部署 n8n

### 2.1 注册 n8n Cloud

1. 打开 n8n.io/cloud → Start free trial
2. 用刚才的 Gmail 注册，选一个"workspace name"（比如 `kizty-intel`）
3. 登录进来后你会看到一个空的工作台（Workflows 列表）

### 2.2 熟悉界面（5 分钟）

n8n 的核心概念只有 3 个：

| 概念 | 通俗解释 |
|---|---|
| **Workflow（工作流）** | 一个流程，就是一串从左到右的积木 |
| **Node（节点）** | 每个积木，比如"定时触发"、"发邮件" |
| **Credential（凭证）** | 存放 API Key 的保险箱，一次配好全局复用 |

左上角 "+" 可以新建工作流。点任何一个节点可以看它的输入输出。右上角有 "Execute Workflow" 按钮手动运行。

---

## 第 3 章 · 配置凭证（一次配置，长期复用）

在 n8n 左侧 → Credentials → "+ Add credential"，依次添加这 3 个：

### 3.1 凭证 A · Google / YouTube

- 类型：选 `Google API`（或直接搜 `YouTube`）
- Authentication：选 `API Key`
- API Key：粘贴你的 YouTube API Key
- 名字起：`YouTube-MVP`
- 保存

### 3.2 凭证 B · Anthropic Claude

- 类型：搜 `Anthropic`
- API Key：粘贴 sk-ant- 开头的那串
- 名字起：`Claude-MVP`
- 保存

> 如果你用的是 DeepSeek/通义/Kimi：用 `OpenAI` 凭证类型，把 Base URL 改成对应厂商的 OpenAI 兼容地址即可（具体见各家文档）。

### 3.3 凭证 C · Gmail / SMTP

- 类型：`SMTP`
- Host：`smtp.gmail.com`（Gmail）/`smtp.163.com`（163）/`smtp.qq.com`（QQ）
- Port：`465` 或 `587`
- User：你的邮箱地址
- Password：**应用专用密码/授权码**（不是登录密码！）
- 名字起：`Mail-MVP`
- 保存

---

## 第 4 章 · 搭建工作流（核心 · 90 分钟）

### 4.1 新建工作流

左侧 Workflows → "+ Create Workflow" → 名字写 `SupplyChain-Daily-Digest`

接下来你要从左到右拖出 6 个节点，连成一条流水线：

```
[定时触发] → [搜 YouTube 视频] → [循环每条视频]
           → [取字幕]          → [Claude 摘要]
           → [汇总发邮件]
```

### 4.2 节点 1 · Schedule Trigger（定时触发）

- 点画布中间的 "+" → 搜 `Schedule Trigger` → 添加
- 配置：
  - Trigger Interval: `Days`
  - Days Between Triggers: `1`
  - Trigger at Hour: `8`（早 8 点）
  - Trigger at Minute: `0`
- 保存节点

### 4.3 节点 2 · YouTube · Search Videos

- 右侧 "+" → 搜 `YouTube` → 选 `YouTube` 节点（不是 YouTube Analytics）
- Credential to connect with：选 `YouTube-MVP`
- Resource：`Video`
- Operation：`Get Many`
- Limit：`20`
- Filters 里点 "Add Filter"：
  - Query（搜索关键词）：填 `supply chain optimization OR APS planning OR "advanced planning"`
  - Published After：`={{ $now.minus({hours: 24}).toISO() }}`（这行是表达式，粘上去就行）
  - Region Code：`US`
  - Relevance Language：`en`
  - Order：`date`
- 保存

> **想加中文源？** 再复制一个 YouTube 节点，Query 换成 `APS 高级排程 OR 供应链数字化`，Region 换成 `CN`，Relevance Language 换成 `zh`。阶段 0 先只跑英文，跑顺了再加。

### 4.4 节点 3 · Filter（过滤垃圾）

- 加 `Filter` 节点
- 条件 1：`{{ $json.snippet.title }}` → `does not contain` → `shorts`
- 条件 2（AND）：`{{ $json.statistics?.viewCount || 0 }}` → `larger than` → `500`（过滤 500 播放以下的）
- 保存

### 4.5 节点 4 · HTTP Request · 拉字幕

YouTube API 本身不直接给字幕，用一个叫 **youtube-transcript.io** 的免费公开接口（或者后面升级用 yt-dlp）。

- 加 `HTTP Request` 节点
- Method：`GET`
- URL：`https://youtubetranscript.com/?server_vid2={{ $json.id.videoId }}`
- Response 里：Enable "Never Error"（失败也往下走，不卡流程）
- 保存

> 阶段 1 我们会换成本地 yt-dlp 方案（更稳），但阶段 0 先用公开接口快速验证。

### 4.6 节点 5 · Anthropic · 摘要

- 加 `Anthropic` 节点（或 `OpenAI` 如果你用国内模型）
- Credential：`Claude-MVP`
- Resource：`Message`
- Model：`claude-haiku-4-5-20251001`（便宜版，做摘要够）
- Messages 里加一条 User 消息，内容粘贴下面这段：

```
你是供应链与 APS 领域的学习助手。下面是一个 YouTube 视频的元数据和字幕片段，请用中文输出：

视频标题：{{ $json.snippet.title }}
频道：{{ $json.snippet.channelTitle }}
发布时间：{{ $json.snippet.publishedAt }}
观看数：{{ $json.statistics.viewCount }}
链接：https://youtube.com/watch?v={{ $json.id.videoId }}

字幕原文：
{{ $('HTTP Request').item.json }}

请输出（严格按格式）：
【一句话摘要】<不超过 50 字>
【3 个核心观点】
1. ...
2. ...
3. ...
【相关性评分】<1-10，10=和供应链/APS 高度相关；低于 6 的请回复"SKIP">
【值得深挖】<一个子话题，可用于后续搜索>
```

- Max Tokens：`800`
- Temperature：`0.3`（低温度保证稳定输出）
- 保存

### 4.7 节点 6 · Code（把所有摘要汇总成一封邮件）

- 加 `Code` 节点
- Language：`JavaScript`
- 粘贴下面这段（你不需要改）：

```javascript
const items = $input.all();
const today = new Date().toISOString().split('T')[0];

const valid = items.filter(i => !i.json.content?.includes('SKIP'));

let html = `<h2>📬 供应链 · 每日精选 · ${today}</h2>`;
html += `<p>今天抓到 ${items.length} 条，AI 筛选出 ${valid.length} 条值得看：</p><hr>`;

valid.forEach((i, idx) => {
  const raw = typeof i.json.content === 'string' ? i.json.content : JSON.stringify(i.json);
  html += `<h3>${idx + 1}. ${raw.match(/视频标题：(.+)/)?.[1] || '(无标题)'}</h3>`;
  html += `<pre style="white-space:pre-wrap;font-family:sans-serif;background:#f7f7f7;padding:12px;border-radius:6px">${raw}</pre>`;
});

html += `<hr><p style="color:#888;font-size:12px">由 web-intel-bot · MVP 阶段 0 自动生成</p>`;

return [{ json: { html, subject: `📬 供应链日报 ${today} · ${valid.length} 条精选` } }];
```

### 4.8 节点 7 · Send Email

- 加 `Send Email` 节点
- Credential：`Mail-MVP`
- From Email：你的发件邮箱
- To Email：你的收件邮箱（可以是同一个）
- Subject：`={{ $json.subject }}`
- Email Format：`HTML`
- HTML：`={{ $json.html }}`
- 保存

### 4.9 连线顺序（从左到右）

```
Schedule Trigger
    ↓
YouTube Search
    ↓
Filter (过滤 shorts 和低播放)
    ↓
HTTP Request (拉字幕)
    ↓
Anthropic (摘要)
    ↓
Code (汇总)
    ↓
Send Email
```

**关键**：在 Anthropic 节点之前，YouTube 搜索出来的是一个数组（20 条），n8n 默认会对每条走一遍后续节点——这正是我们想要的。Code 节点用 `$input.all()` 把所有条汇总成一封邮件。

---

## 第 5 章 · 测试与上线

### 5.1 分段测试（一定要做！）

不要一次跑整条，会浪费 API 额度。按这个顺序分段测：

1. 右键 YouTube Search 节点 → "Execute step" → 看输出有没有 20 条视频
2. 右键 Filter 节点 → Execute → 看过滤后剩几条
3. 右键 HTTP Request → Execute → 看有没有字幕（会有部分失败是正常的）
4. 右键 Anthropic → Execute → **先只跑 1 条**看输出格式对不对
5. 都通过后，右上角 "Execute Workflow" 跑整条
6. 检查邮箱收到没有

### 5.2 常见报错与解决

| 报错 | 原因 | 解决 |
|---|---|---|
| `YouTube: quotaExceeded` | 配额用完 | 等到第二天 0 点（太平洋时间）重置 |
| `Anthropic: 529 overloaded` | 瞬时负载高 | 加 Retry（节点设置里 Retry On Fail）|
| `HTTP Request: 404` | 某个视频没字幕 | 启用 Never Error 后不影响流程 |
| `SMTP: 535 auth failed` | 用了登录密码而非应用密码 | 重新生成应用专用密码 |

### 5.3 打开自动运行

测试通过后，右上角把 `Active` 开关打开（从灰变绿）。从明天早 8 点开始自动跑。

---

## 第 6 章 · 第一周观察（你每天要做的事）

每天早上收到邮件后，花 3 分钟做这件事：

1. 看摘要准不准（Claude 有没有胡说）
2. 看相关性评分合不合理
3. 在一个 Notion 页面或 Excel 里记录：
   - 今天几条、过滤后几条
   - 有几条你觉得真的有用
   - 有几条评分 ≥ 8 但其实没用（假阳性）
   - 有几条评分 < 6 被 SKIP 了但其实有用（漏网之鱼）

这是在积累**评估数据**，第二周我们会用这批数据微调 Prompt。

---

## 第 7 章 · 阶段 0 验收标准

连续 7 天跑完后，自检以下 5 项：

- [ ] 每天 8 点准时收到邮件（7/7 天，允许 1 天失败）
- [ ] 每天至少有 2 条是你觉得值得点开看的
- [ ] AI 摘要和原视频大致对得上，没有明显胡编（抽查 5 条）
- [ ] 月成本 ≤ ¥50（n8n Cloud 还在免费试用里的话可能是 0）
- [ ] 你对"接下来想要什么"有了更具体的想法（比如：想加 Reddit、想加中文源、想做周报）

5 项全过 → 毕业进入**阶段 1**（多源扩展 + PDF 解析）。

达不到也不要气馁，大概率就是以下几件事之一：
- Prompt 不够好 → 调 Prompt（成本几乎 0）
- 关键词没选对 → 换搜索词
- 字幕接口不稳 → 换 yt-dlp 本地方案（需要一台小服务器）

---

## 附录 · 你需要的外部资源一览

| 资源 | 链接关键词 |
|---|---|
| n8n 官方文档 | docs.n8n.io |
| Claude API 文档 | docs.anthropic.com |
| YouTube Data API 文档 | developers.google.com/youtube/v3 |
| n8n 工作流模板市场 | n8n.io/workflows（搜 YouTube 或 Email Digest） |
| 中文 n8n 教程 | B站搜 "n8n 入门"（LIN STEVE 和 AI进化论 做得不错）|

---

## 下一步我们要做什么

等你把这份清单做完（大概周末花半天就能搞定），告诉我：
1. 收到几封邮件，哪几条你觉得好、哪几条差
2. 遇到了哪些卡住的地方

然后我帮你：
- 调 Prompt（专治"摘要不准"）
- 加第二个源（Reddit 或 B 站）
- 开始设计**阶段 1** 的数据库与 PDF 管道

**记住**：阶段 0 的唯一目标是"跑起来 + 每天有东西看"，不追求完美。完美是阶段 2 之后的事。
