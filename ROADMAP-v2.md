# web-intel-bot v2 · 1 周 Roadmap

> **创建于** 2026-04-22 ｜ **基于** `C:\Claude\web-intel-bot`（v1 保持稳定，不动）
>
> **v2 核心变化**：
> 1. 每个主题下区分"海外 / 国内"两栏
> 2. 新增国内 RSS 源（36氪、虎嗅、少数派等）
> 3. UI 改版：主题标题 → 海外/国内两栏分组 → 条目缩进层级
> 4. 为后续 agent 化 + 多用户做技术铺垫

---

## 一、7 天任务拆解

### ⭐ Day 1（今天 2026-04-22）· 国内源接通 + 双栏渲染

**目标**：今晚能跑出一封"每个主题分中外两栏"的邮件。

**任务清单**（我陪你一步步做）：

- [ ] **T1.1** config.yaml 扩展：每个主题加 `sources.rss` 配置，带 `region: cn / intl`（30 分钟）
- [ ] **T1.2** 在 `daily_digest.py` 加 `fetch_rss(topic, url, region)` 函数，返回带 region 字段的 items（1 小时）
- [ ] **T1.3** 调研 + 填入 15 个国内 RSS 源（5 个主题 × 3 个源）（30 分钟）
- [ ] **T1.4** 修改 item 数据结构：每条带 `region` 字段；所有现有 fetcher（youtube/reddit/arxiv/pubmed）统一补 `region: intl`（30 分钟）
- [ ] **T1.5** 改 `render_html`：主题内部分两栏（用 `<table>` 做布局，邮件客户端兼容）（1.5 小时）
- [ ] **T1.6** 加 CSS：主题标题大号 / 中外小分类次级标题 / 条目缩进（30 分钟）
- [ ] **T1.7** dry-run + 发真邮件验证（30 分钟）

**今天完成标志**：你手机收到一封新邮件，每个主题里能看到 "🌐 海外 | 🇨🇳 国内" 两栏各自的条目。

---

### Day 2（明天）· 国内源质量调优

- [ ] **T2.1** 国内 RSS 抓下来的条目，很多会跟主题无关——给每个主题加 `keywords_cn`（中文关键词），预过滤时匹配
- [ ] **T2.2** LLM prompt 针对中文源调整（"中文标题"不用翻译，直接用原标题）
- [ ] **T2.3** 跑 3 天看看效果，淘汰掉没用的 RSS 源

---

### Day 3-4 · 用户配置抽象化

**为什么这步重要**：为将来"给朋友用"铺路，但**此时仍然是单用户**。

- [ ] **T3.1** 抽象出 `subscribers/KIzty.yaml`，主题偏好 / 频率 / 收件邮箱 从 `config.yaml` 移过去
- [ ] **T3.2** `config.yaml` 只保留"全局源定义"（哪些 RSS 可选、哪些 subreddit 可选）
- [ ] **T3.3** 运行时：`python daily_digest.py --user KIzty` 按订阅者配置组装

---

### Day 5-6 · 主题可自定义（产品雏形）

- [ ] **T5.1** 订阅者文件里可以"自定义主题"：`我想看 AI 医疗` → 自动生成 keywords
- [ ] **T5.2** 接入 LLM 自动关键词扩展：给定主题名 + 一句话描述，LLM 吐 15 个关键词

---

### Day 7 · 复盘 + 写 IP 内容

- [ ] **T7.1** 把 v2 升级过程写成一篇 "AI 小白 7 天把自己的脚本升级成产品雏形"
- [ ] **T7.2** 按 `C:\Claude\个人IP-2026-04-21\KIzty语气规范.md` 改 2 轮
- [ ] **T7.3** 发小红书 / B 站 / 即刻

---

## 二、今日（Day 1）详细执行顺序

我会按下面的顺序一个一个干，每完成一个跟你 check：

1. **建 `01-国内RSS源清单.md`**（我调研好 15 个源）
2. **改 `config.yaml`**（加 `rss` 配置段）
3. **daily_digest.py 加 `fetch_rss()` 函数**
4. **所有 fetcher 统一打 region 标签**
5. **改 render_html 渲染双栏**
6. **dry-run 验证**
7. **发邮件验证**

---

## 三、关键设计决策（确认后不变）

### 决策 1：`region` 打在 item 上，不打在 source 上
每条 item 带 `region: "cn" | "intl"` 字段，抓取函数写入。render_html 按 region 分组渲染。
- 好处：config 不用大改，将来加新源只要打个 tag
- 坏处：抓取函数要统一改一遍（一次性成本）

### 决策 2：双栏用 `<table>` 不用 flex/grid
邮件客户端（尤其 Gmail / 网易邮箱）对 CSS flex/grid 支持极差。
- Table 虽然古老，但 iOS Mail / Gmail / Outlook 全都吃
- 栏宽固定 50%，手机上宽度太窄时可以降级成单栏（媒体查询）

### 决策 3：国内 RSS 优先选"公开稳定 + 合规"的
- ✅ 36氪、虎嗅、少数派、机器之心、钛媒体、澎湃新闻
- ❌ 微博、公众号爬虫、需要登录的源（合规风险 + 维护成本高）

### 决策 4：国内源用中文关键词二次过滤
不是所有国内源都跟主题相关，抓回来要用中文关键词过一遍。
- 供应链 → "排产, APS, 供应链, 智能制造"
- AI 大事 → "大模型, AI, 智能体, Claude, GPT"
- 其他类推

---

## 四、长期演进（不在本周）

| 版本 | 核心升级 | 跟 agent 学习路线对应 |
|---|---|---|
| v2.0 ✅ 本周 | 双栏 + 中文源 + 多订阅者配置 | 配合路 3（用现成工具） |
| v2.5 | Web UI 订阅页（Streamlit 最小版） | 开始接触前端 |
| v3.0 | Agent 化：用户用自然语言描述想看啥，agent 自动配置 | = 路 2 的毕业作品 |

---

## 五、跟 v1 的关系

- **v1 位置**：`C:\Claude\web-intel-bot\`（不动！定时任务还是跑这个）
- **v2 位置**：`C:\Claude\web-intel-bot-v2\`（改这个）
- **何时切换**：v2 稳定跑一周后，把定时任务的 bat 路径切过去，老项目归档

---

## 六、进度记录

**今日开始时间**：2026-04-22 ___:___

- T1.1 ☐   完成时间 ___:___
- T1.2 ☐   完成时间 ___:___
- T1.3 ☐   完成时间 ___:___
- T1.4 ☐   完成时间 ___:___
- T1.5 ☐   完成时间 ___:___
- T1.6 ☐   完成时间 ___:___
- T1.7 ☐   完成时间 ___:___

**今日结束**：___:___   能不能收到双栏邮件？ ☐ 能 ☐ 还差点 ☐ 挂了
